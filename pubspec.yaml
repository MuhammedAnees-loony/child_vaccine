name: my_flutter_app
description: A new Flutter project

publish_to: 'none' # Remove this line if you want to publish your package

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  mobile_scanner: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/DSCF0806.JPG
    - assets/images/background.jpg  # Add your background image here
